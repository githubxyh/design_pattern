visit1中的代码，如果添加了除了读写以外的功能，例如支持压缩、提取文件元信息等。
需要修改PdfFile、PPTFile、WordFile等3个类。
1.违背开闭原则，添加一个新的功能，所有类的代码都要修改；
2.虽然功能增多，每个类的代码都不断膨胀，可读性和可维护性都变差了；
3.把所有比较上层的业务逻辑都耦合到PdfFile、PPTFile、WordFile类中，导致这些类的职责不够单一，变成了大杂烩。
